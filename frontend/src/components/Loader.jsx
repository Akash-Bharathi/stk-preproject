function Loader() {

  return (

    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "3rem",
      }}
    >

      <div className="spinner"></div>

    </div>
  );
}

export default Loader;